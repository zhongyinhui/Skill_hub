# Acceptance Checklist

- `SKILL.md` frontmatter `name` 与目录名 `agent-solution-builder` 一致。
- 用户只提供客户名和一句模糊需求时，仍能直接输出可用智能体提示词。
- 默认输出只包含提示词，不输出方案表格、交付计划、测试用例或长解释。
- 提示词内部必须体现业务逻辑推断、工作流、输入理解、输出要求、边界和追问规则。
- 自动写回、外部系统访问、客户承诺和数据权限必须写进提示词边界。
- 不把“做智能体”泛化成万能助手；必须给出具体业务角色和可执行工作流程。
